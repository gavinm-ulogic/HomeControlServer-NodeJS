{
  "name": "homecontrolserver",
  "version": "0.1.0",
  "description": "",
  "main": "app/server.js",
  "dependencies": {
    "@types/node": "^6.0.51",
    "@types/serialport": "^4.0.6",
    "body-parser": "*",
    "cors": "^2.8.1",
    "ds1820-temp": "^1.0.0",
    "express": "*",
    "serialport": "^4.0.6"
  },
  "devDependencies": {
    "concurrently": "*",
    "tslint": "^4.0.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "tsc && concurrently \"tsc -w\"",
    "lint": "tslint ./app/**/*.ts -t verbose --force",
    "start": "tsc && node app/server.js"
  },
  "author": "Gavin Mitchell",
  "license": "ISC"
}
